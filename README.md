# Torrent Manager for the Raspberry Pi (or anywhere)
Network of services containerized with docker compose


## Clean Up Docker and Set Env
`sudo make all`

## Pull and Run Containers
`sudo docker compose up -d`
